document.addEventListener('DOMContentLoaded', function() {
    var reloaderHtml = `
      <div class="reloader">
        <span class="glitch" data-text="digilik">digilik</span>
      </div>
    `;
    document.body.insertAdjacentHTML('afterbegin', reloaderHtml);
  });
  